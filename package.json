{
  "scripts": {
    "dev": "next dev -p 3000 || vite --port 5173",
    "build": "next build || vite build",
    "test": "vitest run",
    "e2e": "playwright test --reporter=html --trace=on",
    "e2e:show": "playwright show-report",
    "dev:check": "node scripts/smoke-ci.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "playwright": "^1.54.2",
    "vitest": "^3.2.4"
  }
}
